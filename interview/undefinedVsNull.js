let x;
console.log(x); // undefined
console.log(typeof x); // undefined

let y = null;
let z;
console.log(x == y); // true - because both of them are data types that represent nothingless
console.log(x === y); // false - different datatypes
