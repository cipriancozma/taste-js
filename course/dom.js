// 14. DOM
// useful to represent the elements of an HTML page
// we can use JS to manipulate the DOM
// object oriented
// accesed using the document property of the window object

// select a single element
// document.getElementById()
// document.querySelector()

// select multiple elements
// document.getElementsByClassName()
// document.getElementsByTagName()
// document.querySelectorAll()

// 15. DOM - going through the DOM
// up -> parentElement,  closest
// siblings -> nextElementSibling, previousElementSibling
// down -> children

// 16. Create dinamic HTML elements
// document.createElement()
// appendChild() -> add the element to DOM
// append / prepend
// insertBefore
// before / after

// 17. Delete and replace HTML elements
// remove() - removeChild()
// replaceWith() - replaceChild()

// 18. EVENTS in JS
// 'click' -> addEventListener()

// 19. EVENTS for mouse
// mouseenter, mouseleave, mousemove, mousedown, mouseup

// 20. EVENTS for keyboard
// keydown, keypress, keyup
